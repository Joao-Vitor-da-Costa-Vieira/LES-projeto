<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>Cartões Cadastrados</title>
</head>
<body>
    <div class="voltar"><a href="/">Voltar</a></div>

    <div class="main">
        <div class="main-cartoes">
            <h2 class="title">Cartões</h2>
            
            <div class="preferencia-cartao">
                <label>Preferencia</label>
                <select name="preferencia" id="preferencia">
                    <% cartoes.forEach(cartao => { %>

                        <option value=""><%=${cartao.crt_bandeira}-${cartao.crt_numero.slice(-4)}%></option>

                    <% }); %>
                </select>
            </div>

            <% cartoes.forEach(cartao => { %>

                <div class="cartoes-mostrado">
                    <div class="cartoes-main">
                        <p class="invisible cartoes-id"><%=cartao.crt_id%></p>
                        <h2><%=cartao.crt_bandeira%></h2>
                        <p><%=cartao.crt_nome%></p>
                        <p><strong>Final: </strong><%=cartao.crt_numero.slice(-4)%></p>
                    </div>
                    <div class="acoes-cartoes">
                        <a class="atualizar" href="#">Atualizar</a>
                        <a href="">excluir</a>
                    </div>
                </div>

            <% }); %>

            <div class="cartoes-adicionar">
                <a href="/cartoes/<%=cartoes[0].usr_id%>/adicionar">Adicionar novo cartão</a>
            </div>
        </div>
    </div>

    <script type="module" src="/scripts/cartoes/cartoes-main.js"></script>
</body>
</html>